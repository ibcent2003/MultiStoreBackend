@model Project.Areas.Setup.Models.DocumentTypeViewModel
@using Project.HtmlHelpers
@{
    ViewBag.Title = "Edit Document Type";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewBag.IsGreetings = "true";
}
@section Crumbs{
    <div class="crumbs">

        <ul class="breadcrumb" id="breadcrumbs">
            <li> <i class="icon-home"></i> <a href="@Url.Action("Index", "Dashboard", new { area="Admin"})">Dashboard</a> </li>    
            <li class="current"> <a title="" href="javascript: return void(0)"></a>Edit Document Type</li>
        </ul>
    </div>
}

<style>
    .required:after {
        content: "*";
        font-weight: bold;
        color: red;
    }
</style>


<div class="row">
    <div class="col-md-12">
        <div class="widget box">
            <div class="widget-header">
                <h4>
                    <i class="icon-reorder"></i>
                    Edit Document Type

                </h4>
            </div>
            <div class="widget-content">
                @using (Html.BeginForm("EditDocumentType", "DocumentType", FormMethod.Post, new { @enctype = "multipart/form-data", @class = "form-horizontal row-border" }))
                {

                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(x=>x.documentTypeform.Id)

                           <div class="form-group">
                        @Html.Label("Document Category", new { @class = "col-md-2 control-label required" })
                        <div class="col-md-6">
                          @Html.DropDownListFor(m => Model.documentTypeform.DocumentCategoryId, new SelectList(Model.CategoryList, "Value", "Text"), "Please select Category", new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.documentTypeform.DocumentCategoryId, null, new { @style = "color:red" })
                        </div>
                    </div>
                    
                    <div class="form-group">
                        @Html.Label("Name", new { @class = "col-md-2 control-label required" })
                        <div class="col-md-6">
                            @Html.TextBoxFor(m => m.documentTypeform.Name, new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.documentTypeform.Name, null, new { @style = "color:red" })
                        </div>
                    </div>

                    <div class="form-group">
                        @Html.Label("Is Deleted?", new { @class = "col-md-2 control-label" })
                        <div class="col-md-6">
                            @Html.CheckBoxFor(m => m.documentTypeform.IsDeleted)
                        </div>
                    </div>


                    <div class="form-actions">
                        <a href="@Url.Action("Index", "DocumentType", new { area="Setup"})" class="btn btn-success btn-xs"><< Back to List </a>
                        <input type="submit" class="btn btn-warning btn-xs pull-right" value="Save" />
                    </div>
                }
            </div>

        </div>
    </div>
</div>